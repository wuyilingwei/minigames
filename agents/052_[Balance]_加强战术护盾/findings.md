# 调研记录

- [当前强度] -> 四次基础值为 100/75/50/25，再乘 `1.18 ^ 难度` -> 保留现有难度曲线和衰减语义，将最终数值统一增加 25%，避免改变四次耐久节奏。
- [价值复用] -> 难度标签、说明和装备推荐均通过 `getEmergencyShieldValues()` / `emergencyShieldValue()` 读取统一 helper -> 只需在单次值 helper 中加入 1.25 倍最终倍率即可同步全部消费方。
