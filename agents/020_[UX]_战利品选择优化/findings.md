# 调研记录

- [推荐标记] -> `classifyRecommendation()` 对任何微小正收益都会进入“谨慎选择”分支 -> 应在该分支前识别输出与生存绝对变化均小于 0.5% 的情况并返回“普通”。
- [自动选择边界] -> 自动动作必须依赖用户主动勾选，且掉落弹窗存在收起、放弃、手动选择和满槽替换流程 -> 计时器需在所有离开当前推荐卡的路径中取消，不能跨弹窗或跨掉落触发。
