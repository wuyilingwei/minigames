# 调研记录

- `pendingLoot` 原先以 `e1/e2` 保存，运行存档校验必须继续接受双件旧存档；新增 `e3` 为可选字段，加载时归一化。
- 普通战斗与魔王临时掉落都经由 `showLootChoice`，因此两处都需要生成第三件。
- 现有部位池保留两个 `oneHand` 条目；在第五槽开放时给所有基础条目权重 `1.5`、`accessory/outerwear` 各 `1`，从而不改变历史单手武器重复条目的意图。
