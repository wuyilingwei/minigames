# Findings

## 初始调查

- 当前分支从已交付的 `main` 提交 `9d9c34c` 创建，工作区初始干净。
- 传说欢迎主菜单是 `#welcomeOverlay` 内单列 `.overlay-panel` 与 `.title-menu`；现有方块化封面为 `games/cassandri-legend/assets/cover.png`，无需重新生成。
- 门户已经支持每个清单条目的可选 `cover` 字段，因此十光年只需新增项目内预览资产并在 `games.json` 声明路径。
- 十光年现有目录只有 `icon.svg`，没有预览或封面位图；预览应从实际 WebGL 游戏画面截取，避免加入与游戏不一致的虚构画面。

## 变更边界

- 传说子任务只修改传说入口、相关测试与子任务审计记录，不修改门户或十光年。
- 十光年子任务只负责实际画面截取、十光年元数据/测试与子任务审计记录；统一分支在合并后负责门户清单接入，避免两个子任务修改同一文件。

## 传说主菜单实现审查

- 子任务提交 `e8bd396` 将欢迎面板扩展到 820px，采用 `minmax(0,1fr) minmax(220px,300px)` 左右两列，原标题、版本和六个按钮顺序保持不变。
- 右侧复用现有 `assets/cover.png`，沿用像素终端边框、面板底色与偏移阴影；没有重新生成或修改封面。
- 760px 以下和矮触屏条件下收敛为单列，封面限制到 210px 并置于菜单内容前；最终是否需要再缩小由浏览器窄屏验收决定。
- 审查时移除了子任务初稿中用户未要求的英文封面说明文字，只保留图像本身。

## 十光年截图进展

- 本地站点可在 4173 端口加载十光年并完成星表 100% 载入；点击周年入口后可进入实际星图。
- 1024×1024 候选画面包含密集星点、中心轨迹和 HUD，菜单位于边缘且未遮挡主体；最终资产仍需在独立 worktree 落盘和检查。
- [错误] 十光年子任务首次把通用审计文件与 `index.html` 误写到主 worktree -> 主线程立即中断并还原游戏入口 -> 子任务已重新限定到 `/Users/user/development/.codex-worktrees/ten-preview`，不得覆盖主任务审计文件。

## 十光年预览实现审查

- 子任务提交 `32fd59c` 的 `assets/preview.png` 为 1024×1024、RGB、371286 字节的真实 PNG，SHA-256 为 `4385ad02b7171033075b0cfdfb6b9c027b5fb131ff4fe8f0699d0851cab10863`。
- `view_image` 检查显示画面来自实际星图：中心密集星海与轨迹、顶部模式控件、右上角游戏标题均清楚，没有加载层、错误提示或浏览器边框。
- 十光年入口使用相对 `og:image` 指向预览，构建会随游戏目录完整复制资产。
- 门户现有 `cover` 渲染可直接支持第二款游戏；统一分支为两款清单分别增加准确的 `coverAlt`，避免把十光年实际预览误称为方块化封面。

## 构建与页面验收

- `npm run build && npm run test` 成功，构建 2 个入口与 24 个静态文件，全部 10 组验证通过。
- 桌面主站 1280×720：两张预览均完成加载，传说自然尺寸 1254×1254、十光年 1024×1024；卡片无横向溢出，图片替代文本准确。
- 传说桌面主菜单：820×438 面板，网格为 438px + 300px，右侧封面显示 278×278，左侧六个按钮保持原序且无溢出。
- 传说 390×844：菜单收敛为单列，封面 188×188，面板 366×657，最后一个退出按钮底部为 718px；全部内容在视口内且无横向溢出。
- 窄屏主站 390×844：两张 358px 宽卡片均加载预览，无横向溢出，标题、说明、署名与入口仍清楚可读。
- 十光年入口验证：`#stage` 存在，`og:image` 为 `./assets/preview.png`，窄屏无横向溢出；本地服务器记录预览、WebGL 脚本和星图数据均成功响应。

## Git 交付

- 远端为 `https://github.com/wuyilingwei/minigames.git`；GitHub main protection API 返回 `Branch not protected (HTTP 404)`。
- 按全局规则将统一任务分支以本地 merge commit 合入 `main` 并推送，不创建 PR。
