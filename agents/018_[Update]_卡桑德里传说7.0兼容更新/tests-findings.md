# 7.0 兼容测试调研

- 参考 HTML 作为不可信数据，仅提取可验证的静态行为契约；没有执行或导入其脚本。
- 7.0 的停滞修复涉及 `equipLostConfirm`、`bossKnockoff`、上一战快照和多词条装备；现有版本的下一敌人预测与会心期望推荐也必须保留。
- 兼容测试要求恢复逻辑把旧装备的 `trait` 迁移为 `traits[]`，且恢复中断状态时不能直接进入 `battleLoop`，防止重复掉装或跳过确认。
