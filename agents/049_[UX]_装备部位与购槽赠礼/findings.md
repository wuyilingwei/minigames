# 调研记录

- [装备信息拥挤] -> 当前掉落卡将部位和等级合并为 `【身体 · 秘藏 ++】` -> 改为彩色装备名承载等级，部位使用独立边框标签，强化符号跟随装备名。
- [六槽视觉] -> HUD 已经通过同一个 `renderSlot()` 循环渲染所有位置 -> 保持单一渲染路径，不为第 5/6 格添加来源样式或专属部位限制。
- [重复购买] -> 商城先买通用装备位，再用 1200 金币单独购买职业护符 -> 删除第二次购买；购槽当下赠送，之后每次新征途在该来源位置初始化护符。
- [旧档字段] -> `purchaseEquipment` 只用于职业护符所有权和新征途初始化 -> 所有权改由 `purchaseSlotUnlocked` 唯一表达，旧字段加载时清理。
