# 调查记录

- 用户确认“自动选择装备”应保留在设置页，问题重点是两个自动开关必须真正驱动游戏流程。
- 项目已有通用 `.pixel-switch` 样式，设置页前三个复选项仅缺少该类名，无需新增另一套控件。
- 自动选择状态属于当前会话，倒计时、闪烁和直接关闭弹窗的逻辑与设置页入口可解耦。
- 自动战斗原先使用未登记的延迟回调；在设置中启停时，旧回调可能跨越暂停边界继续执行，需要统一登记和取消。
- 自动选择在其他弹窗打开时会取消计时，但部分关闭路径没有恢复计时；自动模式因此可能停住。
- 无正收益掉落不能留给玩家手动处理；自动模式应在同一倒计时结束后自动放弃。
