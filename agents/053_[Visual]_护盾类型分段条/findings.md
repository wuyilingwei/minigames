# 调研记录

- [单色无法区分类型] -> 现有我方使用白色单填充，敌方使用紫色单填充 -> 改为同一条内的次数/临时/持续三段，敌我使用相同语义。
- [宽度语义] -> 护盾条长度应表达总护盾相对最大生命，色段表达护盾组成 -> 先计算封顶 100% 的总长度，再按三类数值占比拆分色段。
- [复用路径] -> 主角、主敌人和动态编队卡分别写入宽度 -> 建立单一 `renderShieldMeter()` 路径，不保留旧单色填充兼容。
