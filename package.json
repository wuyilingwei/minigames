{
  "name": "game-archive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node test/build-output.test.mjs",
    "check": "npm run build && npm run test"
  }
}
