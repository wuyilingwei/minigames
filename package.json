{
  "name": "game-archive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node test/build-output.test.mjs && node test/worker-config.test.mjs && node test/cassandri-v7-compat.test.mjs",
    "check": "npm run build && npm run test",
    "deploy": "npm run build && npx wrangler deploy"
  }
}
